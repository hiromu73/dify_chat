

import { Box } from '@mui/material'
import React from 'react'

const chatpage = () => {
  return (
    <Box sx={{ display: 'flex', alignItems: 'center', borderRadius: '20px', border: '1px solid #ccc', overflow: 'hidden', height}}>
    </Box>
  )
}

export default chatpage
